# Copy to backend.hcl (git-ignored) and fill in your values.
# Then run: terraform init -backend-config=backend.hcl

bucket         = "your-tf-state-bucket-name"
key            = "eventstracker/prod/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "terraform-state-lock"
encrypt        = true
